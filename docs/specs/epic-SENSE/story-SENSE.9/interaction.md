# SENSE.9 交互

首页顶层能力区点击「感知中心」打开窗体。该区域与应用启动器、项目、角色、技能平行，不属于应用启动器。窗体导航：感知源、触发规则、事件记录、运行状态。新增来源按平台展示专属表单；Email 配 mailbox/poll，Webhook 配 callback 信息，Stream 配连接状态。规则采用“来源与事件 → 条件 → 目标与 ownership → 权限/HITL → 预览启用”向导。错误就地展示安全 code，secret 输入提交后立即清空。

键盘可遍历、焦点可见、状态不只依赖颜色；窄窗体改为顶部选择器与单列内容。
