# 开发文档 - Story AG.2

**版本:** 1.0；**最后更新:** 2026-09-11
**状态:** AG2-T1 提案准备阶段；尚未修改应用源码。

## 开发目标

完成 34 处当前边界违规修复，保持运行契约。按 Story 模板裁剪未涉及的新库、新状态管理和新页面章节。

## 实施步骤

批准后按 [tasks.md](../../../../openspec/changes/fix-reported-architecture-violations/tasks.md) 在隔离 Task worktree 实施 UI 与 Core 两个工作包，合并至 Proposal 完成回归，再合并 dev。

## 环境与第三方库

使用项目现有 Node/pnpm、TypeScript、Vitest、ESLint、Next/Electron 构建链，不新增依赖。只修改源码和打包脚本，不修改生成产物。

## 审查要点

依赖方向正确；单一契约与实现；无动态导入绕过；所有启动/恢复入口完整组装；持久化与授权不变；生产打包可加载。

## 开发日志与证据

2026-09-11：更新旧 AG.2 文档，创建新 Proposal 和验收用例。实现提交、测试命令、输出与最终 import 迁移将在实施后追加；当前无完成证据。

## 部署与回滚

全部必需用例通过后按既有构建脚本生成本地测试产物并记录路径。失败不合并；需要回滚时回退 Proposal 提交，无用户数据迁移。
