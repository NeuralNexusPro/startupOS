# SENSE.13 测试

## 自动化验证 Goal

实施完成后创建并完成：“通过 SENSE.13 感知中心应用入口、Dock 启动、连接状态聚合与快速面板测试 case”。

| ID | 场景 | 预期 |
|---|---|---|
| S13-UI-01 | 首页应用启动器 | 显示“感知与连接”，排序在工作区后，独立横幅不存在 |
| S13-UI-02 | 点击卡片 | 调用 `openSenseCenter()`，不复制窗体逻辑 |
| S13-DCK-01 | 固定并从 Dock 启动 | action 元数据保留，打开/聚焦同一窗体 |
| S13-STS-01 | 无连接/全部停用 | 显示“尚未连接”，提供管理入口 |
| S13-STS-02 | 全部正常 | 显示正常状态及连接数量 |
| S13-STS-03 | 部分/全部异常 | 分别显示警告/中断及异常数量 |
| S13-STS-04 | 加载/API 失败 | 显示加载/未知状态，不泄露异常或 Secret |
| S13-A11Y-01 | 键盘和读屏 | 按钮有状态 aria-label，Enter/Space 打开，Esc 关闭并恢复焦点 |
| S13-RWD-01 | 窄屏 | 面板不溢出，状态不只依赖颜色 |
| S13-SKL-01 | 点击“创建技能” | 位于创建入口组，并通过 `skill-creator-app` 通用 Skill 链路启动 |

执行 Vitest 组件/配置测试、Web typecheck、相关 ESLint、`git diff --check` 和 Core 反向依赖扫描。未配置浏览器 E2E 时记录人工步骤与风险。

## 自动化结果（2026-09-07）

- 感知中心、首页配置与 Dock 专项：6 个测试文件、24 项通过（含“创建技能”入口）。
- Web TypeScript：通过；相关 ESLint 0 error、34 个既有/非阻断 warning。
- 独占感知横幅移除扫描、Core→Desktop 反向依赖扫描及 `git diff --check`：通过。
- 浏览器 E2E 未配置；人工检查顶部面板定位、窗口聚焦及 Electron 独立 Dock 跨窗口行为仍是剩余风险。
