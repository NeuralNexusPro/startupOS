diff --git a/dist/client.cjs b/dist/client.cjs
index e195cd22aea578d285cd9268e11b891594c36019..f94c6c4be79e2c4b5a4355b784911478c728e178 100644
--- a/dist/client.cjs
+++ b/dist/client.cjs
@@ -67,7 +67,7 @@ class DWClient extends EventEmitter {
       subscriptions: subscriptions.map((subscription) => ({ ...subscription }))
     };
     if (!this.config.clientId || !this.config.clientSecret) {
-      console.error("clientId or clientSecret is null");
+      (this.config.logger ?? console).error("clientId or clientSecret is null");
       throw new Error("clientId or clientSecret is null");
     }
     if (!Number.isInteger(this.config.maxPendingEventHandlers) || this.config.maxPendingEventHandlers <= 0) {
@@ -89,7 +89,7 @@ class DWClient extends EventEmitter {
   printDebug(msg) {
     if (this.debug) {
       const date = "[" + (/* @__PURE__ */ new Date()).toISOString() + "]";
-      console.info(date, msg);
+      (this.config.logger ?? console).info(date, msg);
     }
   }
   registerAllEventListener(onEventReceived) {
@@ -98,7 +98,7 @@ class DWClient extends EventEmitter {
   }
   registerCallbackListener(eventId, callback) {
     if (!eventId || !callback) {
-      console.error(
+      (this.config.logger ?? console).error(
         "registerCallbackListener: eventId and callback must be defined"
       );
       throw new Error(
@@ -147,7 +147,7 @@ class DWClient extends EventEmitter {
       if (messageId) {
         inflightMessageIds.delete(messageId);
       }
-      console.warn(`Callback listener failed for topic ${eventId}`, err);
+      (this.config.logger ?? console).warn(`Callback listener failed for topic ${eventId}`, err);
       return;
     }
     if (!result) {
@@ -156,7 +156,7 @@ class DWClient extends EventEmitter {
       }
       return;
     }
-    const task = Promise.resolve(result).catch((err) => console.warn(`Callback listener failed for topic ${eventId}`, err));
+    const task = Promise.resolve(result).catch((err) => (this.config.logger ?? console).warn(`Callback listener failed for topic ${eventId}`, err));
     this.callbackTasks.add(task);
     void task.finally(() => {
       this.callbackTasks.delete(task);
@@ -166,7 +166,7 @@ class DWClient extends EventEmitter {
     });
   }
   [EventEmitter.captureRejectionSymbol](err, eventName) {
-    console.warn(`Callback listener failed for topic ${String(eventName)}`, err);
+    (this.config.logger ?? console).warn(`Callback listener failed for topic ${String(eventName)}`, err);
   }
   async getAccessToken() {
     const result = await axios.get(
@@ -264,7 +264,7 @@ class DWClient extends EventEmitter {
         this.eventAbortControllers.set(socket, new AbortController());
       } catch (err) {
         this.printDebug("WebSocket constructor error");
-        console.warn("ERROR", err);
+        (this.config.logger ?? console).warn("ERROR", err);
         reject(err);
         return;
       }
@@ -300,14 +300,14 @@ class DWClient extends EventEmitter {
         opened = true;
         this.connected = true;
         this.reconnectAttempts = 0;
-        console.info("[" + (/* @__PURE__ */ new Date()).toISOString() + "] connect success");
+        (this.config.logger ?? console).info("[" + (/* @__PURE__ */ new Date()).toISOString() + "] connect success");
         if (this.config.keepAlive) {
           this.isAlive = true;
           this.heartbeatIntervallId = setInterval(() => {
             if (this.socket !== socket)
               return;
             if (this.isAlive === false) {
-              console.error(
+              (this.config.logger ?? console).error(
                 "TERMINATE SOCKET: Ping Pong does not transfer heartbeat within heartbeat intervall"
               );
               return socket.terminate();
@@ -343,7 +343,7 @@ class DWClient extends EventEmitter {
         if (this.socket !== socket)
           return;
         this.printDebug("SOCKET ERROR");
-        console.warn("ERROR", err);
+        (this.config.logger ?? console).warn("ERROR", err);
         rejectOnce(err);
         if (socket.readyState === WebSocket.OPEN || socket.readyState === WebSocket.CONNECTING) {
           socket.terminate();
@@ -390,6 +390,7 @@ class DWClient extends EventEmitter {
     } catch (err) {
       this.printDebug("Connect failed: " + (err instanceof Error ? err.message : String(err)));
       if (!this.userDisconnect) {
+        this.config.logger?.warn("Connect failed", err);
         this.reconnectAttempts++;
         this.scheduleReconnect();
       }
@@ -399,7 +400,7 @@ class DWClient extends EventEmitter {
     }
   }
   disconnect() {
-    console.info("Disconnecting.");
+    (this.config.logger ?? console).info("Disconnecting.");
     this.userDisconnect = true;
     if (this.reconnectTimerId) {
       clearTimeout(this.reconnectTimerId);
@@ -432,7 +433,7 @@ class DWClient extends EventEmitter {
           break;
       }
     } catch (err) {
-      console.warn("Failed to process downstream message", err);
+      (this.config.logger ?? console).warn("Failed to process downstream message", err);
     }
   }
   sendFrame(socket, payload, onSent) {
@@ -443,12 +444,12 @@ class DWClient extends EventEmitter {
     try {
       socket.send(JSON.stringify(payload), (err) => {
         if (err) {
-          console.warn("Failed to send websocket response", err);
+          (this.config.logger ?? console).warn("Failed to send websocket response", err);
         }
         onSent == null ? void 0 : onSent();
       });
     } catch (err) {
-      console.warn("Failed to send websocket response", err);
+      (this.config.logger ?? console).warn("Failed to send websocket response", err);
       onSent == null ? void 0 : onSent();
     }
   }
@@ -494,7 +495,7 @@ class DWClient extends EventEmitter {
         this.cacheEventResult(messageId, ackData);
       }
     }).catch((err) => {
-      console.warn("Failed to cache event result", err);
+      (this.config.logger ?? console).warn("Failed to cache event result", err);
     }).finally(() => {
       this.eventTasks.delete(task);
       if (messageId && this.inflightEvents.get(messageId) === task) {
@@ -507,7 +508,7 @@ class DWClient extends EventEmitter {
       if (!(signal == null ? void 0 : signal.aborted)) {
         this.sendEventAck(message, ackData, socket);
       }
-    }).catch((err) => console.warn("Failed to process event", err));
+    }).catch((err) => (this.config.logger ?? console).warn("Failed to process event", err));
   }
   getCachedEventResult(messageId) {
     const cached = this.eventResults.get(messageId);
@@ -604,11 +605,11 @@ class DWClient extends EventEmitter {
     try {
       ackData = await this.onEventReceived(message, signal);
       if (!ackData || ackData.status !== "SUCCESS" /* SUCCESS */ && ackData.status !== "LATER" /* LATER */) {
-        console.warn("Event listener returned an invalid acknowledgement");
+        (this.config.logger ?? console).warn("Event listener returned an invalid acknowledgement");
         ackData = { status: "LATER" /* LATER */ };
       }
     } catch (err) {
-      console.warn("Event listener failed", err);
+      (this.config.logger ?? console).warn("Event listener failed", err);
       ackData = { status: "LATER" /* LATER */ };
     }
     return ackData;
@@ -625,7 +626,7 @@ class DWClient extends EventEmitter {
   }
   send(messageId, value) {
     if (!messageId) {
-      console.error("send: messageId must be defined");
+      (this.config.logger ?? console).error("send: messageId must be defined");
       throw new Error("send: messageId must be defined");
     }
     const msg = {
@@ -656,7 +657,7 @@ class DWClient extends EventEmitter {
   }
   sendGraphAPIResponse(messageId, value) {
     if (!messageId) {
-      console.error("send: messageId must be defined");
+      (this.config.logger ?? console).error("send: messageId must be defined");
       throw new Error("send: messageId must be defined");
     }
     const msg = {
diff --git a/dist/client.d.ts b/dist/client.d.ts
index 77cff9f54fc39d74005a77de967cae72ed7ced37..4b5b3f5fd1ccf645cef8c43ff20f0b286fb04c5f 100644
--- a/dist/client.d.ts
+++ b/dist/client.d.ts
@@ -15,6 +15,7 @@ interface DWClientConfig {
     clientSecret: string;
     keepAlive?: boolean;
     debug?: boolean;
+    logger?: Pick<Console, "info" | "warn" | "error">;
     ua?: string;
     endpoint?: string;
     access_token?: string;
@@ -84,6 +85,7 @@ declare class DWClient extends EventEmitter {
         ua?: string;
         keepAlive?: boolean;
         debug?: boolean;
+        logger?: DWClientConfig["logger"];
         autoReconnect?: boolean;
         maxPendingEventHandlers?: number;
         maxPendingCallbackHandlers?: number;
@@ -99,6 +101,7 @@ declare class DWClient extends EventEmitter {
         clientSecret: string;
         keepAlive?: boolean | undefined;
         debug?: boolean | undefined;
+        logger?: DWClientConfig["logger"];
         ua?: string | undefined;
         endpoint?: string | undefined;
         access_token?: string | undefined;
diff --git a/dist/client.mjs b/dist/client.mjs
index 613441e36ac189b9476a545b71fc3ceed3b176c6..8a1df61203ceb8170728c1774dd475fd8783f3f4 100644
--- a/dist/client.mjs
+++ b/dist/client.mjs
@@ -65,7 +65,7 @@ class DWClient extends EventEmitter {
       subscriptions: subscriptions.map((subscription) => ({ ...subscription }))
     };
     if (!this.config.clientId || !this.config.clientSecret) {
-      console.error("clientId or clientSecret is null");
+      (this.config.logger ?? console).error("clientId or clientSecret is null");
       throw new Error("clientId or clientSecret is null");
     }
     if (!Number.isInteger(this.config.maxPendingEventHandlers) || this.config.maxPendingEventHandlers <= 0) {
@@ -87,7 +87,7 @@ class DWClient extends EventEmitter {
   printDebug(msg) {
     if (this.debug) {
       const date = "[" + (/* @__PURE__ */ new Date()).toISOString() + "]";
-      console.info(date, msg);
+      (this.config.logger ?? console).info(date, msg);
     }
   }
   registerAllEventListener(onEventReceived) {
@@ -96,7 +96,7 @@ class DWClient extends EventEmitter {
   }
   registerCallbackListener(eventId, callback) {
     if (!eventId || !callback) {
-      console.error(
+      (this.config.logger ?? console).error(
         "registerCallbackListener: eventId and callback must be defined"
       );
       throw new Error(
@@ -145,7 +145,7 @@ class DWClient extends EventEmitter {
       if (messageId) {
         inflightMessageIds.delete(messageId);
       }
-      console.warn(`Callback listener failed for topic ${eventId}`, err);
+      (this.config.logger ?? console).warn(`Callback listener failed for topic ${eventId}`, err);
       return;
     }
     if (!result) {
@@ -154,7 +154,7 @@ class DWClient extends EventEmitter {
       }
       return;
     }
-    const task = Promise.resolve(result).catch((err) => console.warn(`Callback listener failed for topic ${eventId}`, err));
+    const task = Promise.resolve(result).catch((err) => (this.config.logger ?? console).warn(`Callback listener failed for topic ${eventId}`, err));
     this.callbackTasks.add(task);
     void task.finally(() => {
       this.callbackTasks.delete(task);
@@ -164,7 +164,7 @@ class DWClient extends EventEmitter {
     });
   }
   [EventEmitter.captureRejectionSymbol](err, eventName) {
-    console.warn(`Callback listener failed for topic ${String(eventName)}`, err);
+    (this.config.logger ?? console).warn(`Callback listener failed for topic ${String(eventName)}`, err);
   }
   async getAccessToken() {
     const result = await axios.get(
@@ -262,7 +262,7 @@ class DWClient extends EventEmitter {
         this.eventAbortControllers.set(socket, new AbortController());
       } catch (err) {
         this.printDebug("WebSocket constructor error");
-        console.warn("ERROR", err);
+        (this.config.logger ?? console).warn("ERROR", err);
         reject(err);
         return;
       }
@@ -298,14 +298,14 @@ class DWClient extends EventEmitter {
         opened = true;
         this.connected = true;
         this.reconnectAttempts = 0;
-        console.info("[" + (/* @__PURE__ */ new Date()).toISOString() + "] connect success");
+        (this.config.logger ?? console).info("[" + (/* @__PURE__ */ new Date()).toISOString() + "] connect success");
         if (this.config.keepAlive) {
           this.isAlive = true;
           this.heartbeatIntervallId = setInterval(() => {
             if (this.socket !== socket)
               return;
             if (this.isAlive === false) {
-              console.error(
+              (this.config.logger ?? console).error(
                 "TERMINATE SOCKET: Ping Pong does not transfer heartbeat within heartbeat intervall"
               );
               return socket.terminate();
@@ -341,7 +341,7 @@ class DWClient extends EventEmitter {
         if (this.socket !== socket)
           return;
         this.printDebug("SOCKET ERROR");
-        console.warn("ERROR", err);
+        (this.config.logger ?? console).warn("ERROR", err);
         rejectOnce(err);
         if (socket.readyState === WebSocket.OPEN || socket.readyState === WebSocket.CONNECTING) {
           socket.terminate();
@@ -388,6 +388,7 @@ class DWClient extends EventEmitter {
     } catch (err) {
       this.printDebug("Connect failed: " + (err instanceof Error ? err.message : String(err)));
       if (!this.userDisconnect) {
+        this.config.logger?.warn("Connect failed", err);
         this.reconnectAttempts++;
         this.scheduleReconnect();
       }
@@ -397,7 +398,7 @@ class DWClient extends EventEmitter {
     }
   }
   disconnect() {
-    console.info("Disconnecting.");
+    (this.config.logger ?? console).info("Disconnecting.");
     this.userDisconnect = true;
     if (this.reconnectTimerId) {
       clearTimeout(this.reconnectTimerId);
@@ -430,7 +431,7 @@ class DWClient extends EventEmitter {
           break;
       }
     } catch (err) {
-      console.warn("Failed to process downstream message", err);
+      (this.config.logger ?? console).warn("Failed to process downstream message", err);
     }
   }
   sendFrame(socket, payload, onSent) {
@@ -441,12 +442,12 @@ class DWClient extends EventEmitter {
     try {
       socket.send(JSON.stringify(payload), (err) => {
         if (err) {
-          console.warn("Failed to send websocket response", err);
+          (this.config.logger ?? console).warn("Failed to send websocket response", err);
         }
         onSent == null ? void 0 : onSent();
       });
     } catch (err) {
-      console.warn("Failed to send websocket response", err);
+      (this.config.logger ?? console).warn("Failed to send websocket response", err);
       onSent == null ? void 0 : onSent();
     }
   }
@@ -492,7 +493,7 @@ class DWClient extends EventEmitter {
         this.cacheEventResult(messageId, ackData);
       }
     }).catch((err) => {
-      console.warn("Failed to cache event result", err);
+      (this.config.logger ?? console).warn("Failed to cache event result", err);
     }).finally(() => {
       this.eventTasks.delete(task);
       if (messageId && this.inflightEvents.get(messageId) === task) {
@@ -505,7 +506,7 @@ class DWClient extends EventEmitter {
       if (!(signal == null ? void 0 : signal.aborted)) {
         this.sendEventAck(message, ackData, socket);
       }
-    }).catch((err) => console.warn("Failed to process event", err));
+    }).catch((err) => (this.config.logger ?? console).warn("Failed to process event", err));
   }
   getCachedEventResult(messageId) {
     const cached = this.eventResults.get(messageId);
@@ -602,11 +603,11 @@ class DWClient extends EventEmitter {
     try {
       ackData = await this.onEventReceived(message, signal);
       if (!ackData || ackData.status !== "SUCCESS" /* SUCCESS */ && ackData.status !== "LATER" /* LATER */) {
-        console.warn("Event listener returned an invalid acknowledgement");
+        (this.config.logger ?? console).warn("Event listener returned an invalid acknowledgement");
         ackData = { status: "LATER" /* LATER */ };
       }
     } catch (err) {
-      console.warn("Event listener failed", err);
+      (this.config.logger ?? console).warn("Event listener failed", err);
       ackData = { status: "LATER" /* LATER */ };
     }
     return ackData;
@@ -623,7 +624,7 @@ class DWClient extends EventEmitter {
   }
   send(messageId, value) {
     if (!messageId) {
-      console.error("send: messageId must be defined");
+      (this.config.logger ?? console).error("send: messageId must be defined");
       throw new Error("send: messageId must be defined");
     }
     const msg = {
@@ -654,7 +655,7 @@ class DWClient extends EventEmitter {
   }
   sendGraphAPIResponse(messageId, value) {
     if (!messageId) {
-      console.error("send: messageId must be defined");
+      (this.config.logger ?? console).error("send: messageId must be defined");
       throw new Error("send: messageId must be defined");
     }
     const msg = {
