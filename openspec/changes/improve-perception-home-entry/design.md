# Design：感知中心双层入口

首页卡片承担可发现与 Dock 固定；顶部状态按钮承担连接监控。两者统一调用 `openSenseCenter()`，感知中心保持单例窗体。

状态从既有 Zustand store 的 connector/health/event 摘要派生，不在 `page.tsx` 写业务逻辑。面板使用正常、警告、中断、未配置、未知五态，颜色与文字并用；通知仍只表达事件结果。

保留整行横幅会挤压核心内容；只放顶部栏首次用户难发现；只放应用卡片无法感知故障。双层入口在发现性和低干扰监控间平衡。
